{% extends 'base.html.twig' %}

{% block title %}Inviter un utilisateur{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4">📧 Inviter un utilisateur dans le groupe "{{ workGroup.name }}"</h1>

    {{ form_start(form) }}
        <div class="mb-3">
            {{ form_label(form.invitedUser) }}
            {{ form_widget(form.invitedUser, {'attr': {'class': 'form-control select2'}}) }}
            {{ form_errors(form.invitedUser) }}
        </div>

        <button type="submit" class="btn btn-primary">Envoyer l'invitation</button>
    {{ form_end(form) }}
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            $('.select2').select2({
                theme: 'bootstrap-5',
                width: '100%'
            });
        });
    </script>
{% endblock %}

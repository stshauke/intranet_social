{% extends 'base.html.twig' %}

{% block title %}Préférences de notification{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow rounded">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">⚙️ Préférences de notification</h4>
                </div>

                <div class="card-body">
    {{ form_start(form) }}

    <div class="form-group row mb-4 align-items-center">
        <div class="col-md-4">
            {{ form_label(form.new_post, null, { 'label_attr': { 'class': 'form-label fw-bold' } }) }}
        </div>
        <div class="col-md-8">
            {{ form_widget(form.new_post, { 'attr': {'class': 'form-check-input'} }) }}
        </div>
    </div>

    <div class="form-group row mb-4 align-items-center">
        <div class="col-md-4">
            {{ form_label(form.new_comment, null, { 'label_attr': { 'class': 'form-label fw-bold' } }) }}
        </div>
        <div class="col-md-8">
            {{ form_widget(form.new_comment, { 'attr': {'class': 'form-check-input'} }) }}
        </div>
    </div>

    <div class="form-group row mb-4 align-items-center">
        <div class="col-md-4">
            {{ form_label(form.new_like, null, { 'label_attr': { 'class': 'form-label fw-bold' } }) }}
        </div>
        <div class="col-md-8">
            {{ form_widget(form.new_like, { 'attr': {'class': 'form-check-input'} }) }}
        </div>
    </div>

    <div class="form-group row mb-4 align-items-center">
        <div class="col-md-4">
            {{ form_label(form.mention, null, { 'label_attr': { 'class': 'form-label fw-bold' } }) }}
        </div>
        <div class="col-md-8">
            {{ form_widget(form.mention, { 'attr': {'class': 'form-check-input'} }) }}
        </div>
    </div>

    <div class="text-end">
        <button type="submit" class="btn btn-success">
            💾 Enregistrer mes préférences
        </button>
    </div>

    {{ form_end(form) }}
</div>

            </div>
        </div>
    </div>
</div>
{% endblock %}

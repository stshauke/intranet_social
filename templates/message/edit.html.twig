{% extends 'base.html.twig' %}

{% block title %}Modifier le message{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">✏️ Modifier le message</h5>
                </div>

                <div class="card-body">
                    {{ form_start(form) }}
                        <div class="mb-4">
                            {{ form_label(form.content, 'Contenu du message', {'label_attr': {'class': 'form-label fw-bold'}}) }}
                            {{ form_widget(form.content, {'attr': {'class': 'form-control', 'rows': 6, 'placeholder': 'Modifiez votre message ici...'}}) }}
                        </div>

                        <button class="btn btn-success w-100">💾 Mettre à jour</button>
                    {{ form_end(form) }}
                </div>

                <div class="card-footer text-end">
                    <a href="{{ path('app_message') }}" class="btn btn-outline-secondary">⬅️ Retour à la messagerie</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
